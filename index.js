// Login checker
let userName = prompt('Who are you?');
if (input = 'Admin') {
    let pass = prompt('Password', ' ');
    if (pass = 'TheMaster') {
        alert('Welcome');
    }
    else if (pass = '' || pass = null)
}